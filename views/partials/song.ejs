<ol>
    <% list.forEach(song => { %>
    <li>
        <div class="rankbox">
            <span class="rank"><%- `# ${song.rank}` %></span>
            <span><%- `(${song.streams} streams)` %></span>
        </div>
        <div class="title_and_singer">
            <span class="artist"><%= song.title %></span>
            <span><%= song.artist %></span>
        </div>
        <span class="date"><%= `Date Published: ${song.publicationDate}` %></span>
    </li>
    <% }); %>
</ol>